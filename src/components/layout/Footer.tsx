import React from "react";

/**
 * React component for the Footer Section.
 */
export const Footer = () => {
  return (
    <footer className="footer">
      <p>
        To place a number in a square — type the number on your keyboard whilst
        hovering over the square, or after clicking on it
      </p>
    </footer>
  );
};
